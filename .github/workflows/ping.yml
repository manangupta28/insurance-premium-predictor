name: Keep Streamlit App Awake

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: HEAD Ping to Streamlit App
        run: curl -I https://insurance-premium-predictor-uutxfqqfqw3g3id3sq5knj.streamlit.app/

